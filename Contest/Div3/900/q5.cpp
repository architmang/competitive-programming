#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n;
    cin>>n;
    vector<int> a;
    a.resize(n);
    for(int i=0;i<n;i++) cin>>a[i];
    int q;
    
}

int main() {
    int t;
    cin >> t;
    while (t--) solve();
    return 0;
}
