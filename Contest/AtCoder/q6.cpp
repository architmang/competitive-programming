#include <bits/stdc++.h>
using namespace std;

int dp[100][100][100];

int rec(int u, int p, int k)
{
    if(dp[])
}

void solve() {
    int n, x, k;
    cin>>n>>x>>k;
    int p = (int)(x/2);
    cout<<rec(x, p, k)<<endl;
    return;    
}

int main() {
    int q,k;
    cin>>q>>k;
    map<int, int> mp;
    while(q--)
    {
        string s;
        cin>>s;
        int k = s[1];
        if(find(mp.begin(), mp.end(), ))
        solve();
    }
    return 0;
}
